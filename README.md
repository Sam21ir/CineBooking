# Cinema Booking App Landing Page

A modern cinema booking application landing page built with React, TypeScript, and Tailwind CSS.

## Getting Started

### Installation

Run `npm i` to install the dependencies.

### Development

Run `npm run dev` to start the development server.

### Build

Run `npm run build` to build the project for production.